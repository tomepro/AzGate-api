// Define la clase 'RecoveryItemDTO' que representa un Data Transfer Object para los ítems de recuperación
export class RecoveryItemDTO {
  // Define la propiedad 'id' de tipo número como solo lectura
  readonly id: number;
  // Define la propiedad 'guid' de tipo número como solo lectura
  readonly guid: number;
  // Define la propiedad 'itemEntry' de tipo número como solo lectura
  readonly itemEntry: number;
}