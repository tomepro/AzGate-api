// Define la clase 'CharactersDto' que representa un Data Transfer Object para los personajes
export class CharactersDto {
  // Define la propiedad 'guid' de tipo número como solo lectura
  readonly guid: number;
}