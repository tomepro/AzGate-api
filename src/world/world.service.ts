import { Injectable } from '@nestjs/common';

@Injectable() // Define el servicio WorldService como un proveedor de NestJS. Esto permite que el servicio sea inyectado en otros componentes.
export class WorldService { // Define una clase WorldService que contiene la lógica de negocio relacionada con el mundo del juego.
  // TODO
}
